% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/synthetic_tune.R
\name{synthetic_tune}
\alias{synthetic_tune}
\title{Iterate through Variation for Synthetic Data Tuning}
\usage{
synthetic_tune(
  structure,
  variations,
  n = 5,
  model = "GPT-4.1",
  base_url = "https://litellmproxy.osu-ai.org/"
)
}
\arguments{
\item{structure}{The output from the \code{describe_structure} function telling the generator what columns and datatypes to use.}

\item{variations}{String vector describing a different variation.}

\item{n}{Number of rows to generate per dataset.}

\item{model}{The model to use for the chat (defaults to "gpt-4.1")}

\item{base_url}{The base URL to the endpoint. Default is "https://litellmproxy.osu-ai.org".}
}
\value{
A list of data.frames by variation.
}
\description{
Generates multiple synthetic datasets using different variations or scenarios, allowing you to tune and compare outputs.
}
\examples{
structure <- describe_structure(mtcars)
synth <- generate_synthetic_sample(n = 2, structure = structure, prompt = "Simulate realistic car performance data with similar distributions.")
variations <- c("more variance", "include some missing values")
tuned <- synthetic_tune(structure, variations)

synth1 <- tuned[[1]]
real_var <- sapply(mtcars, var)
synthetic_var <- sapply(synth1, var)
tibble(variable = names(real_var), real = real_var, synthetic = synthetic_var, ratio = synthetic / real)
}
